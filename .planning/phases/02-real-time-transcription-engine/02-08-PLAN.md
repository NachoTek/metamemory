---
phase: 02-real-time-transcription-engine
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/metamemory/widgets/floating_panels.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Manual scroll pauses auto-scroll for ~10 seconds"
    - "Auto-scroll resumes after pause expires"
    - "Auto-scroll does not fight user during manual scroll"
  artifacts:
    - path: "src/metamemory/widgets/floating_panels.py"
      provides: "Scroll pause detection and timer"
      contains: "_auto_scroll_paused, _pause_timer"
    - path: "src/metamemory/widgets/floating_panels.py"
      provides: "Scroll value change signal connection"
      contains: "verticalScrollBar().valueChanged.connect"
  key_links:
    - from: "user scroll action"
      to: "_auto_scroll_paused flag"
      via: "valueChanged signal handler"
      pattern: "detect user scroll, set pause flag, start timer"
    - from: "_scroll_to_bottom"
      to: "conditional scroll logic"
      via: "pause state check"
      pattern: "if not self._auto_scroll_paused: scrollToBottom()"
---

## Objective

Implement auto-scroll pause functionality: when user manually scrolls up, pause auto-scroll for 10 seconds to allow reading.

**Purpose:** Current behavior immediately fights user scroll by scrolling back down, making it impossible to read previous transcript content.

**Output:** Smart auto-scroll that detects manual scroll and pauses for 10 seconds before resuming.

## Execution Context

@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-real-time-transcription-engine/02-UAT.md

**Gap Analysis:**
- UAT Test 8 failed: Manual scroll pause not working
- Root cause: `FloatingTranscriptPanel` has no scroll detection or pause mechanism
- Line 151: `scroll_timer` unconditionally scrolls every 100ms
- Line 220: `_scroll_to_bottom()` called on every transcript update
- Lines 262-265: `_scroll_to_bottom()` unconditionally sets scrollbar to maximum

**Current broken flow:**
1. User scrolls up manually
2. Auto-scroll timer triggers 100ms later
3. `_scroll_to_bottom()` forces scroll back to bottom
4. User cannot read previous content

## Tasks

<task type="auto">
  <name>Implement scroll pause detection mechanism</name>
  <files>src/metamemory/widgets/floating_panels.py</files>
  <action>
    Add scroll pause detection to `FloatingTranscriptPanel`:
    
    1. Add instance variables in `__init__`:
       - `self._auto_scroll_paused = False`
       - `self._pause_timer = QTimer(self)`
       - `self._pause_timer.setSingleShot(True)`
       - `self._pause_timer.timeout.connect(self._resume_auto_scroll)`
       - `self._last_scroll_value = 0`
       - `self._is_at_bottom = True`
    
    2. Connect to scrollbar value changed signal in `__init__`:
       - `self.verticalScrollBar().valueChanged.connect(self._on_scroll_value_changed)`
    
    3. Implement `_on_scroll_value_changed(self, value)`:
       - Check if value < maximum (user scrolled up from bottom)
       - If scrolled up: set `self._auto_scroll_paused = True`
       - Start pause timer: `self._pause_timer.start(10000)` (10 seconds)
       - Update `_last_scroll_value`
    
    4. Implement `_resume_auto_scroll(self)`:
       - Set `self._auto_scroll_paused = False`
       - Immediately scroll to bottom to catch up
    
    5. Modify `_scroll_to_bottom(self)`:
       - Add check: `if self._auto_scroll_paused: return`
       - Only scroll if not paused
       
    6. Modify scroll timer callback:
       - Only call `_scroll_to_bottom()` if not paused
  </action>
  <verify>
    1. Run `python -c "from metamemory.widgets.floating_panels import FloatingTranscriptPanel; print('Import OK')"`
    2. Check for syntax errors
    3. Verify QTimer import exists
  </verify>
  <done>
    - _auto_scroll_paused flag added
    - _pause_timer QTimer added with 10s timeout
    - valueChanged signal connected
    - _on_scroll_value_changed handler implemented
    - _resume_auto_scroll callback implemented
    - _scroll_to_bottom checks pause state
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Test scroll pause functionality</name>
  <what-built>Auto-scroll pause when user manually scrolls up</what-built>
  <how-to-verify>
    1. Launch app: `python -m metamemory`
    2. Click record button
    3. Speak several sentences to generate transcript content
    4. Wait for transcript to fill panel (scroll bar should appear)
    5. Scroll up manually using mouse wheel or scroll bar
    6. Verify:
       - Panel stays scrolled up (does NOT immediately jump back down)
       - You can read previous content
    7. Continue speaking new content
    8. Verify new content appears but panel stays at your scroll position
    9. Wait 10 seconds without scrolling
    10. Verify auto-scroll resumes and jumps to bottom
    11. Verify auto-scroll continues working normally after resume
  </how-to-verify>
  <resume-signal>Type "verified" if scroll pause works correctly, or describe issues</resume-signal>
</task>

## Verification

- [ ] `_auto_scroll_paused` flag tracks pause state
- [ ] `_pause_timer` QTimer set to 10 seconds
- [ ] `valueChanged` signal connected to scroll handler
- [ ] Scroll handler detects upward scroll and triggers pause
- [ ] `_scroll_to_bottom()` respects pause state
- [ ] Timer resume works correctly
- [ ] UAT Test 8 passes: Manual scroll pauses auto-scroll

## Success Criteria

1. **Pause on Manual Scroll:** Scrolling up manually pauses auto-scroll immediately
2. **10-Second Timeout:** After 10 seconds of no manual scroll, auto-scroll resumes
3. **No Fighting:** During pause, transcript updates don't force scroll back to bottom
4. **UAT Test 8 Pass:** User can read previous content while recording continues

## Output

After completion, create `.planning/phases/02-real-time-transcription-engine/02-08-SUMMARY.md`
