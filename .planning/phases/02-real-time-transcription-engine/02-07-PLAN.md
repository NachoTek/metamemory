---
phase: 02-real-time-transcription-engine
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/metamemory/transcription/accumulating_processor.py
  - src/metamemory/recording/controller.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Transcript file contains text without repetition"
    - "Only new/changed segments are added to transcript"
    - "Buffer transcription does not cause text accumulation"
  artifacts:
    - path: "src/metamemory/transcription/accumulating_processor.py"
      provides: "Segment tracking and deduplication"
      contains: "_last_segment_index"
    - path: "src/metamemory/recording/controller.py"
      provides: "Filtered segment emission"
      contains: "only new words added"
  key_links:
    - from: "accumulating_processor._process_buffer"
      to: "controller._on_transcription"
      via: "result callback"
      pattern: "only emit segments after _last_segment_index"
---

## Objective

Fix transcript text repetition by implementing segment tracking to only emit new/changed segments from the accumulating processor.

**Purpose:** Transcript files currently contain repeating text that grows continuously as the same audio buffer gets re-transcribed.

**Output:** Transcription processor that tracks already-emitted segments and only outputs new content.

## Execution Context

@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-real-time-transcription-engine/02-UAT.md

**Gap Analysis:**
- UAT Test 9 failed: File contains repeating text that accumulates
- Root cause: `AccumulatingTranscriptionProcessor` re-transcribes entire buffer every 2 seconds
- Line 227: `self._phrase_bytes += chunk_bytes` - buffer accumulates continuously
- Lines 346-379: ALL segments output every cycle
- No tracking of which segments were already emitted

**Example of current bug:**
```
"Testing one two."
"Testing one two testing."
"Testing one two testing... Hello, this is a test"
```

## Tasks

<task type="auto">
  <name>Implement segment index tracking in processor</name>
  <files>src/metamemory/transcription/accumulating_processor.py</files>
  <action>
    Add segment tracking to prevent duplicate emission:
    
    1. Add instance variable `_last_emitted_segment_index = -1` in `__init__`
    
    2. Modify `_process_buffer()` or result handling (around line 341-379):
       - Track how many segments have been emitted so far
       - After transcription, only process segments with index > _last_emitted_segment_index
       - Update _last_emitted_segment_index to current max index
    
    3. On silence detection (when buffer is cleared):
       - Reset _last_emitted_segment_index = -1
       - This ensures new phrases start fresh
    
    4. The result callback should receive only NEW segments, not the full list
    
    Key constraint: whisper.cpp returns full transcription, so we must filter on our side.
  </action>
  <verify>
    1. Run `python -c "from metamemory.transcription.accumulating_processor import AccumulatingTranscriptionProcessor; print('Import OK')"`
    2. Check for syntax errors
    3. Run unit tests: `python -m pytest tests/test_accumulating_processor.py -v` (if exists)
  </verify>
  <done>
    - _last_emitted_segment_index tracking variable added
    - Segment filtering logic implemented
    - Reset on silence implemented
  </done>
</task>

<task type="auto">
  <name>Verify controller handles filtered segments correctly</name>
  <files>src/metamemory/recording/controller.py</files>
  <action>
    Ensure the controller properly handles partial segment results:
    
    1. Check `_on_transcription()` method (lines 380-392)
    2. Verify it appends words correctly when receiving only new segments
    3. If controller maintains its own state, ensure it's compatible with filtered input
    
    The controller should receive only new segments and append them to the transcript store without duplicate checking (since processor now handles that).
  </action>
  <verify>
    Code review of controller.py lines 380-392 to confirm it handles partial results.
  </verify>
  <done>
    - Controller correctly appends new segments
    - No duplicate logic conflicts with processor changes
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Test transcription without repetition</name>
  <what-built>Segment deduplication to prevent repeating text in transcript files</what-built>
  <how-to-verify>
    1. Launch app: `python -m metamemory`
    2. Click record button
    3. Speak: "Testing one two three"
    4. Wait 5 seconds
    5. Speak: "This is a second phrase"
    6. Wait 5 seconds
    7. Stop recording
    8. Open transcript file from recordings directory
    9. Verify:
       - "Testing one two three" appears only once (not repeated)
       - "This is a second phrase" appears only once
       - No text accumulation like "Testing one two three Testing one two three"
  </how-to-verify>
  <resume-signal>Type "verified" if transcript contains no repetition, or describe remaining issues</resume-signal>
</task>

## Verification

- [ ] `_last_emitted_segment_index` tracking implemented in processor
- [ ] Only new segments emitted after each transcription cycle
- [ ] Index reset on silence detection
- [ ] Controller correctly handles filtered segments
- [ ] Transcript file contains no repeating text

## Success Criteria

1. **No Repetition:** Speaking "Testing one two" twice (with pause) does not produce "Testing one two Testing one two" in transcript
2. **Clean Output:** Transcript file contains exactly what was spoken, without accumulation
3. **UAT Test 9 Pass:** File contains accurate text without repetition

## Output

After completion, create `.planning/phases/02-real-time-transcription-engine/02-07-SUMMARY.md`
