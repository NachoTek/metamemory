---
phase: 02-real-time-transcription-engine
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/metamemory/widgets/floating_panels.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Settings panel opens without crashing when clicking settings lobe"
    - "Settings panel can dock to widget edges like transcript panel"
  artifacts:
    - path: "src/metamemory/widgets/floating_panels.py"
      provides: "FloatingSettingsPanel.dock_to_widget method"
      contains: "def dock_to_widget"
  key_links:
    - from: "main_widget.py:525"
      to: "FloatingSettingsPanel.dock_to_widget"
      via: "method call"
      pattern: "settings_panel.dock_to_widget"
---

## Objective

Fix the AttributeError crash when opening settings panel by adding the missing `dock_to_widget` method to `FloatingSettingsPanel`.

**Purpose:** Unblock the settings functionality which is currently unreachable due to a crash.

**Output:** Working `dock_to_widget` method in `FloatingSettingsPanel` that allows settings panel to dock to widget edges.

## Execution Context

@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-real-time-transcription-engine/02-UAT.md

**Gap Analysis:**
- UAT Test 2 failed: `AttributeError: 'FloatingSettingsPanel' object has no attribute 'dock_to_widget'`
- Root cause: `FloatingSettingsPanel` class (lines 302-420) missing `dock_to_widget` method
- Reference implementation exists in `FloatingTranscriptPanel` (lines 118-144)
- main_widget.py line 525 calls `dock_to_widget()` on settings panel

## Tasks

<task type="auto">
  <name>Add dock_to_widget method to FloatingSettingsPanel</name>
  <files>src/metamemory/widgets/floating_panels.py</files>
  <action>
    Add `dock_to_widget(self, widget, position)` method to `FloatingSettingsPanel` class.
    
    Reference the working implementation in `FloatingTranscriptPanel` (lines 118-144):
    - Copy the logic for calculating edge proximity (20px threshold)
    - Copy the snap-to-edge positioning logic
    - Copy the docked state management
    - Copy the show_4_5ths() call when docked
    - Copy the geometry calculations based on widget position and screen size
    
    Key elements to implement:
    1. Check proximity to screen edges (top, bottom, left, right)
    2. If within 20px of edge, snap and show 4/5ths hidden
    3. Position panel adjacent to widget
    4. Ensure panel is not positioned off-screen
    5. Update docked state flag
    
    Match the exact API signature: `dock_to_widget(self, widget, position)`
  </action>
  <verify>
    1. Run `python -c "from metamemory.widgets.floating_panels import FloatingSettingsPanel; print('Import OK')"`
    2. Verify no syntax errors in floating_panels.py
    3. Run `python -m metamemory` and click settings lobe - panel should open without crash
  </verify>
  <done>
    - Settings panel opens without AttributeError
    - dock_to_widget method exists and is callable
    - Panel can dock to widget edges
  </done>
</task>

<task type="auto">
  <name>Verify settings panel functionality</name>
  <files>src/metamemory/widgets/floating_panels.py</files>
  <action>
    Test the complete settings panel flow:
    1. Verify panel displays hardware info (RAM, CPU cores)
    2. Verify model selection dropdown works
    3. Verify settings persist after closing and reopening
    
    This validates the fix works and unblocks UAT Tests 2 and 3.
  </action>
  <verify>
    Manual verification steps:
    1. Launch app: `python -m metamemory`
    2. Click settings lobe (gear icon)
    3. Verify panel opens showing hardware info
    4. Change model selection (e.g., tiny → base)
    5. Close settings panel
    6. Reopen settings panel - verify selection persisted
  </verify>
  <done>
    - Settings panel displays correctly
    - Model selection works
    - Settings persist across restarts
  </done>
</task>

## Verification

- [ ] `FloatingSettingsPanel.dock_to_widget()` method exists
- [ ] Settings panel opens without crash when clicking settings lobe
- [ ] Hardware detection displays correctly (RAM, CPU cores, model recommendation)
- [ ] Model selection dropdown functions correctly
- [ ] Settings persist across application restarts

## Success Criteria

1. **Crash Fixed:** Settings panel opens without `AttributeError: 'FloatingSettingsPanel' object has no attribute 'dock_to_widget'`
2. **Functionality Verified:** UAT Test 2 passes (hardware detection display)
3. **Dependency Unblocked:** UAT Test 3 (model selection persistence) can now be tested

## Output

After completion, create `.planning/phases/02-real-time-transcription-engine/02-06-SUMMARY.md`
