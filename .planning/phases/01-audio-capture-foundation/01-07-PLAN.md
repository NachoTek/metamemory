---
phase: 01-audio-capture-foundation
plan: 07
type: execute
wave: 1
depends_on: ["06"]
files_modified: []
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Widget source lobes (Mic/System) respond to single click to toggle"
  artifacts:
    - path: "src/metamemory/widgets/main_widget.py"
      provides: "ToggleLobeItem click handlers receive events"
      references: "01-06-PLAN.md fix"
  key_links:
    - from: "MeetAndReadWidget.mouseReleaseEvent"
      to: "ToggleLobeItem.mousePressEvent"
      via: "event propagation (fixed in 01-06)"
      pattern: "event propagation to child items"
---

<objective>
Verify Gap 3 fix - Widget lobes single-click functionality.

Purpose: This gap has the same root cause as Gap 2 (parent widget event.accept() blocking events) and is already fixed by plan 01-06.
Root cause: Same parent event handling issue - resolved in plan 01-06.
Output: Verification that lobes respond to single click after 01-06 fix.
</objective>

<execution_context>
@C:\Users\david.keymel\.config\opencode/get-shit-done/workflows/execute-plan.md
@C:\Users\david.keymel\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-audio-capture-foundation/01-UAT.md

# Gap being closed
- truth: "Widget source lobes (Mic/System) should respond to single click to toggle"
- status: failed
- reason: "Lobes do not respond to single click. They work with double click."
- root_cause: "Same as record button - parent widget event.accept() blocks all single-click events from reaching interactive items."

# Fix location
This gap is identical to Gap 2 and is fixed by plan 01-06:
- 01-06-PLAN.md replaces mousePressEvent with mouseReleaseEvent + click detection
- This fix allows events to propagate to RecordButtonItem, ToggleLobeItem, and SettingsLobeItem

# No code changes needed
The fix in 01-06 addresses this gap completely. This plan exists only to:
1. Document that this gap is covered by 01-06
2. Verify the fix works for lobes (not just record button)
3. Create SUMMARY.md for tracking gap closure
</context>

<tasks>

<task type="auto">
  <name>Verify lobe single-click functionality after 01-06 fix</name>
  <files></files>
  <action>
No code changes required. This task verifies that the fix in 01-06 (mouseReleaseEvent with click detection) works for ToggleLobeItem.

The fix in 01-06 ensures that:
1. MeetAndReadWidget no longer accepts events on interactive items
2. Events propagate to RecordButtonItem, ToggleLobeItem, and SettingsLobeItem
3. Click vs drag is distinguished by <5px movement and <300ms elapsed

ToggleLobeItem already has correct mousePressEvent handler (lines 481-487):
```python
def mousePressEvent(self, event):
    """Toggle state."""
    if event.button() == Qt.MouseButton.LeftButton:
        self.is_active = not self.is_active
        self.update()
        print(f"{self.lobe_type} toggled: {self.is_active}")
        event.accept()
```

After 01-06 fix, this handler will receive events and work correctly.

Verification (manual test in next step):
1. Launch widget
2. Click Mic lobe ONCE - verify it toggles active/inactive
3. Click System lobe ONCE - verify it toggles active/inactive
4. Ensure lobes still don't start drag (click vs drag detection)
  </action>
  <verify># Verification is manual - see verification section below</verify>
  <done>ToggleLobeItem single-click verified working after 01-06 fix</done>
</task>

</tasks>

<verification>
After 01-06 is complete and this task is verified:
1. Launch widget with python -m meetandread.main
2. Click Mic lobe ONCE - verify it toggles active/inactive state (visual feedback)
3. Click System lobe ONCE - verify it toggles active/inactive state
4. Click Settings lobe ONCE - verify it responds (console output when implemented)
5. Ensure lobes don't start dragging when clicked (click vs drag detection working)
</verification>

<success_criteria>
- Mic lobe toggles with single click
- System lobe toggles with single click
- Settings lobe responds to single click
- No double-click requirement for any interactive items
</success_criteria>

<output>
After completion, create `.planning/phases/01-audio-capture-foundation/01-07-SUMMARY.md`
Note: SUMMARY should indicate "No code changes - fixed by 01-06"
</output>
