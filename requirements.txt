# meetandread requirements

# GUI Framework
PyQt6>=6.4.0

# Audio Processing
sounddevice>=0.4.6  # WASAPI via PortAudio
numpy>=1.24.0       # Frame conversion + mixing
soxr>=0.3.0         # Resampling
comtypes>=1.2.0     # Windows COM interface for WASAPI loopback
pyaudiowpatch>=0.2.12  # WASAPI loopback capture via patched PortAudio

# AI/ML - Whisper
# Using whisper.cpp via pywhispercpp to avoid PyTorch DLL issues on Windows
pywhispercpp>=1.3.0    # Python bindings for whisper.cpp (GGML-based, no PyTorch)
# Note: faster-whisper removed due to WinError 1114 (PyTorch DLL init failure)

# Speaker Diarization
sherpa-onnx>=1.10.0    # ONNX-based speaker diarization + embedding (Windows compatible)

# System utilities
psutil>=5.9.0

# Development
pytest>=7.0.0
black>=22.0.0
mypy>=1.0.0